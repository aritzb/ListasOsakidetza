# Claude Workspace

This folder holds local instructions and reusable prompts for AI-assisted work in this repo.

## Structure

- `agents/` reusable agent role templates
- `skills/` focused repo knowledge and workflows

## How to Use

- Start with `AGENTS.md` and `DESIGN.md` at the repo root.
- Use the files here as modular context for future tasks.

